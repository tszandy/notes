$(document).ready(function() {
}

# The remove() method removes the matching element from the DOM
$(selector).remove(selector)

# The prepend() method is used to insert the specified content at the beginning of the selected element.
$(selector).prepend(content_to_be_inserted)

# The append() method inserts the specified content at the end of the selected elements.
$(selector).append(content_to_append)

