# internationalization services
# locale module opens access to the POSIX locale database and functionality
import locale

# returns the database of the local conventions as a dictionary
locale.localeconv()

locale.getlocale(category)

